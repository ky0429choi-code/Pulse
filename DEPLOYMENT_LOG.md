# Pulse Dashboard 배포 및 트러블슈팅 리포트

본 문서는 GAS(Google Apps Script) 기반의 커스텀 대시보드를 최적화하고 배포하는 과정에서 발생한 주요 이슈와 해결 방법을 정리한 마크다운 문서입니다. 향후 동일한 형태의 서버/클라이언트 연동 시 발생할 수 있는 문제를 예방하기 위한 지침서로 활용할 수 있습니다.

---

## 🚀 1. 배포 및 통신 연동 (CLASP 도입)

* **도입 배경**: 백엔드 소스 코드가 여러 파일(`AuthService.gs`, `Schema.gs` 등)로 분할되고 파일 크기가 방대해지면서 운영 코드에 에러가 발생할 때마다 롤백이나 부분 수정이 매우 까다로워졌습니다.
* **해결 방법**: `npm install -g @google/clasp`를 통해 로컬 터미널과 구글 서버를 동기화했습니다.
* **결과**: `.clasp.json` 파일에 타겟 구글 스크립트 ID를 입혀두었기 때문에, 앞으로는 코드 수정 후 터미널에 `clasp push` 명령어 하나만 치면 2~3초 만에 29개의 로컬 시스템 코드가 클라우드로 일괄 덮어씌워집니다.

---

## 🛠️ 2. 발생한 에러와 해결 과정 (Troubleshooting)

### 이슈 1. "함수 없음" (Apps Script 에디터 비활성화)
* **현상**: 구글 Apps Script 화면에서 수동으로 초기 세팅 함수를 구동하려 했으나, 상단 메뉴바의 함수 목록이 `함수 없음`으로 뜨고 실행 버튼이 눌리지 않았습니다.
* **원인**: 백엔드 코드가 높은 수준의 보안 캡슐화(IIFE 패턴, `var Installer = (function(){...})();`)로 작성되어 있어, 구글 에디터가 내부 함수들을 찾지 못했습니다.
* **해결**: `Installer.gs` 파일 맨 밑에 UI 노출 전용 전역 함수(`function runSetup() { return Installer.setupAll(); }`)를 추가하여 에디터에서 선택 및 실행할 수 있도록 연결했습니다.

### 이슈 2. "SpreadsheetApp.openById 권한 없음"
* **현상**: 프론트엔드에서 데이터를 요청하자 백엔드 통신 오류 창이 나타났습니다.
* **원인**: 외부(깃허브 페이지)에서 구글 스프레드시트에 접근하려 했기 때문에, 구글의 보안 정책이 발동해 접근을 일시적으로 차단한 것입니다.
* **해결**: Apps Script 에디터 내에서 `runSetup` 함수를 수동으로 1회 실행하여 "권한 검토 -> 보안 경고 무시 -> 허용"이라는 구글 OAuth 인증 절차를 자발적으로 통과시켜 차단을 풀었습니다.

### 이슈 3. "Missing required headers in '권한관리'"
* **현상**: 권한관리 탭을 읽어오는 과정에서 에러가 발생했습니다.
* **원인**: 기존 프로토타입 시절 엑셀 1행에 적어두셨던 한글 제목(`접속권한계정`, `개인비밀번호`)과, 이번에 엄격하게 보안 매핑한 영문 논리 키워드(`userId`, `password`)가 일치하지 않아 시스템이 해당 열을 찾지 못했습니다. (데이터 보호를 위해 시트를 강제로 덮어쓰지 않고 에러 발생)
* **해결**: `Schema.gs` 로직 테이블을 통째로 뜯어고쳐, 기존의 한글 제목표(`접속권한계정` 등 10개)를 시스템이 원격으로 완벽하게 추적 및 자동 매핑할 수 있도록 브릿지를 연결했습니다. 시트 내용을 유지한 채로 해결되었습니다.

### 이슈 4. "Failed to fetch" (로그인 화면 튕김 현상)
* **현상**: 로그인 버튼 클릭 시 완전히 차단되는 CORS 에러가 발생했습니다.
* **원인**: `clasp push`를 통해 코드를 업로드하면서, 기본적으로 포함되어 있어야 하는 웹 접근 권한 설정(`webapp.access: ANYONE_ANONYMOUS`)이 누락된 채 배포되었습니다. 이에 따라 익명 사용자의 API 접근이 구글 로그인 페이지로 강제 리다이렉트되면서 발생한 통신 단절이었습니다.
* **해결**: `appsscript.json` 파일에 웹 앱 실행자 권한(`executeAs: USER_DEPLOYING`) 및 접근 권한(`access: ANYONE_ANONYMOUS`) 속성을 살려 넣고, `clasp deploy -i [기존배포ID] -d "V8"` 명령어로 고정된 웹 앱 버전 위로 강제 덮어쓰기 배포를 끝마쳤습니다.

---

## 🎯 3. 최종 상태 점검
- 프론트엔드(`welstory-docs-v1/docs`): UI 번역 적용 완료, `app.js` 캐시 전환 및 배포(GitHub Pages) 완료.
- 백엔드(Apps Script Version 8): 캐싱 최적화, 스케줄러 트리거, API 통신 보안(CORS Open) 모두 정상 반영.
- 현재 대시보드 로그인 화면부터 스프레드시트 통신 전 구간이 에러 없이 무결성 상태로 연동되었습니다.
