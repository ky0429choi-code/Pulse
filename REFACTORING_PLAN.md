# 대시보드 백데이터 구조 재조정 및 가공 계획 (Data Architecture Restructuring)

현재 다운로드해주신 21개의 프로토타입 원본 데이터를 기반으로, 대시보드의 퍼포먼스와 확장성을 극대화하기 위해 불필요한 데이터를 가지치기(추려서 가공)하고 백엔드 아키텍처를 전면 재조정하는 계획입니다. 
제가 구글 스프레드시트의 원본 상황을 몰랐기 때문에 그동안 시스템을 작고 단순하게 만들었지만, 이제 원본 데이터의 규모를 파악했으므로 그에 걸맞은 "진짜" 엔터프라이즈 구조로 업그레이드합니다.

## User Review Required
> [!IMPORTANT]
> 아래의 설계에 따라 **운영에 필수적인 코어 시트들만 선택적으로 채택**하고 나머지 더미 시트들(ex. `시트9`, `리포트캐시`, `테이크아웃품목` 등)은 백엔드 로직에서 제외하여 시스템 속도를 높이려고 합니다. 제외되면 안 되는 특정 시트가 있다면 미리 공사 전에 말씀해 주시면 계획에 반영하겠습니다!

## Proposed Changes

---

### [Architecture & Schema]
기존의 단순했던 `CONFIG.SHEETS` 매핑을 운영계 수준으로 격상시키고, 하드코딩된 설정들을 실제 DB 기반으로 전환합니다.

#### 1. `gas/Config.gs` 재설계
- 기존의 10개였던 `CONFIG.SHEETS` 설정을 개편하여 `실적데이터`, `사업장현황`, `월간지표_압축`, `AI예측이력`을 핵심 코어 데이터베이스 영역으로 공식 편입시킵니다.
- 하드코딩 되어있던 `SITE_LIST_JSON` 설정을 폐기합니다.

#### 2. `gas/Schema.gs` 재설계
- 새로 편입되는 엑셀 시트들의 헤더(날짜, 지역, 사업장명, DI_조식 등)를 시스템이 정확히 파싱할 수 있도록 `SCHEMAS` 객체를 재설계 및 추가합니다.

---

### [Repositories & Data Layer]
새로 연결될 코어 시트들을 자유자재로 읽고 쓰기 위한 전담 데이터 리포지토리(DB 서빙 모듈)를 신설합니다.

#### 3. `gas/repositories/SiteRepository.gs` 신규 생성
- `사업장현황` 시트를 단일 마스터 DB로 삼아, 하드코딩 없이 전국의 모든 지점 상세 정보(조리사 수, 좌석 수, 매출 목표 등)를 조회하는 전담 모듈입니다. 프론트엔드의 그 드롭다운에 H1, H2 대신 이 시트에서 읽은 100% 리얼 데이터가 자동으로 나갑니다.

#### 4. `gas/repositories/MetricsRepository.gs` 신규 생성
- `실적데이터`, `월간지표_압축`, `AI예측이력` 탭 데이터를 융합하여 프론트엔드의 대시보드 차트들이 요구하는 포맷으로 가공해 주는 모듈입니다.

#### 5. `gas/repositories/UserRepository.gs` 점검
- 수정한 `권한관리` 엑셀 탭 구조와 완전히 일치하도록 로직을 재점검하고 마이그레이션합니다.

---

### [API & Router]
프론트엔드와 통신하는 API 네트워크 규격을 원본 데이터베이스 규모에 맞게 무한 확장합니다.

#### 6. `gas/Router.gs` 통신망 확장
- 프론트엔드가 "나 새로고침했어, 사업장 목록 줘!" 라고 요구할 때 `SiteRepository`를 거쳐 동적으로 데이터를 내려주는 `getSiteList` 전용 엔드포인트를 개통합니다.
- 복잡한 AI 예측 및 오차율 정보를 한 번에 넘겨주는 `getDashboardMetrics` 엔드포인트도 추가합니다.

---

## Open Questions
> [!WARNING]
> 현재 이 구조 개편 작업(리팩토링)을 진행하는 동안에는 기존의 가짜 데이터(`Site H1`, `Site H2`) 연결이 끊어지고 진짜 데이터를 붙이는 대공사 과정이므로, 중간에 잠시 대시보드의 일부 데이터가 비어보일 수 있습니다. (정상적인 과정입니다.)
> 
> 이 설계도 내용이 마음에 드신다면 채팅창에 **승인(혹은 진행해, OK 등)** 의사를 남겨놔주시면 즉각 대규모 백엔드 공사에 돌입하겠습니다!
